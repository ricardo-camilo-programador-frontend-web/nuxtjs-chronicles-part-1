{
  "compilerOptions": {
    "incremental": true, // Habilita builds incrementais, salvando informações da build anterior para compilações mais rápidas.
    "tsBuildInfoFile": ".tsbuildinfo", // Arquivo onde os dados de builds incrementais são armazenados.
    "target": "ES2020", // Define a versão do ECMAScript para o código JavaScript gerado. Recomenda-se sempre usar a versão mais moderna suportada pelo ambiente.
    "jsx": "preserve", // Mantém o código JSX nos arquivos para que futuras etapas (como o bundler) possam processá-lo.
    "lib": ["ES2022", "DOM", "DOM.Iterable"], // Especifica quais bibliotecas globais (ex: ES2020, DOM) estarão disponíveis durante a compilação. Garante que as APIs mais recentes estejam acessíveis.
    "useDefineForClassFields": true, // Controla como os campos de classe são inicializados; padrão para a especificação mais nova do JavaScript.
    "module": "ESNext", // Define que os módulos seguirão o padrão ECMAScript mais moderno, permitindo import/export dinâmico e otimizações mais recentes.
    "moduleResolution": "bundler", // Define o modo de resolução de módulos para projetos que usam bundlers modernos como Vite, esbuild, etc.
    "paths": {
      "~/*": ["./src/*"],
      "@icons/*": ["./src/components/icons/*"]
    }, // Cria atalhos personalizados para importação de módulos, facilitando organização e manutenção do código.
    "resolveJsonModule": true, // Permite importar arquivos .json diretamente via import, tipando seu conteúdo automaticamente.
    "allowJs": true, // Faz o compilador aceitar e analisar também arquivos JavaScript além dos TypeScript.
    "checkJs": true, // Se true, valida arquivos .js também, levantando possíveis problemas; false desabilita essa checagem.
    "strict": true, // Ativa todas as checagens estritas, maximizando a qualidade e segurança do código TypeScript.
    "noFallthroughCasesInSwitch": true, // Gera erro caso algum case de switch seja deixado “aberto” sem break ou return, evitando bugs sutis.
    "noUnusedLocals": true, // Aponta variáveis locais declaradas mas não usadas, mantendo o código limpo.
    "noUnusedParameters": true, // Aponta parâmetros de funções não utilizados, ajudando a evitar confusão.
    "declaration": false, // Não gera arquivos de declaração (.d.ts) na build. Útil se o projeto não será consumido como lib.
    "declarationMap": false, // Não gera arquivos de mapeamento de declarações, mantendo o build mais leve.
    "downlevelIteration": true, // Garante que operações de iteração funcionem corretamente em targets antigos, implementando polyfills quando necessário.
    "importHelpers": true, // Otimiza o código gerado ao usar helpers do TypeScript compartilhados (evita duplicação nos arquivos emitidos).
    "noEmit": true, // Não gera arquivos de saída ao compilar; geralmente usado quando o foco é apenas checagem de tipos.
    "removeComments": false, // Mantém os comentários no JavaScript gerado, útil para documentação ou debug.
    "sourceMap": true, // Gera arquivos de mapa de origem (.map), essenciais para um debug eficiente no navegador.
    "allowSyntheticDefaultImports": true, // Permite importar módulos CommonJS usando sintaxe de default import, mesmo que eles não tenham um default export declarado.
    "esModuleInterop": true, // Facilita interoperabilidade entre módulos ES6 e CommonJS, simplificando importações.
    "isolatedModules": true, // Faz com que cada arquivo seja tratado como um módulo independente, facilitando integrações com bundlers modernos.
    "skipLibCheck": true // Pula a checagem de tipos de arquivos de declaração (.d.ts) de dependências, acelerando a build e evitando problemas irrelevantes.
  },
  "references": [
    {
      "path": "./tsconfig.node.json"
    }
  ], // Aponta para outros arquivos de configuração tsconfig, permitindo compartilhamento de configurações e builds multi-projeto.
  "include": ["src/**/*", "types/**/*.d.ts", "nuxt.config.ts", "app.config.ts", "vite-env.d.ts", "nuxt.d.ts", ".nuxt/**/*.d.ts"], // Lista de padrões de arquivos/folders a serem considerados na compilação pelo TypeScript.
  "exclude": ["node_modules", "dist", ".output"] // Lista de pastas a serem desconsideradas da compilação (inclusive builds/bundles e dependências).
}
